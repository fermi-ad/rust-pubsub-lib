name: CI-CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  run-tests-and-coverage:
    uses: fermi-ad/.github/.github/workflows/rust-integration.yaml@v1
    secrets: inherit
    permissions:
      contents: write
      pull-requests: write
    with:
      static-dependencies: 'libcurl4-openssl-dev libsasl2-dev'
      
  deploy-docs:
    needs: run-tests-and-coverage
    if: ${{ github.event_name == 'push' }}
    uses: fermi-ad/.github/.github/workflows/deploy-rust-docs.yaml@v1
    secrets: inherit 
    permissions:
      contents: write
      id-token: write
      pages: write
    with:
      static-dependencies: 'libcurl4-openssl-dev libsasl2-dev'
